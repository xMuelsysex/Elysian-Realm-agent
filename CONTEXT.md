# Domain Context

## Terms

### 剧情原文
- **Definition**：游戏剧情中按原始顺序保存的完整文本内容，包括每段对话对应的说话角色和原文台词。
- **Rules/Invariants**：剧情原文是正史资料，不以摘要替代；说话角色、台词内容和先后顺序属于文本事实，角色引用时不能把其他角色的台词说成自己的经历或发言。

### 剧情阶段
- **Definition**：参与者在目标故事中的当前进度位置，用来判断哪些剧情已经发生、哪些剧情仍属于未来。
- **Rules/Invariants**：角色只知道当前阶段及其之前被允许知道的剧情；阶段推进后才能获得后续剧情知识，未到达的剧情不能进入角色可用知识。

### 角色剧情知识
- **Definition**：某个角色在特定剧情阶段下可以知道、回忆或回应的正史内容子集。
- **Rules/Invariants**：角色剧情知识由剧情阶段和角色视角共同决定；正史知识与角色亲身经历记忆是两种不同事实，知道剧情不等于亲身经历过剧情。

### 目标剧情范围
- **Definition**：本次剧情知识建设覆盖的两个故事章节：`往世乐土` 与 `致以无瑕之人`。
- **Rules/Invariants**：纳入两大章的全部剧情类型，包括主线、支线、卡牌内容和战斗结果等；章节内部保持原文的章节、场景、对话顺序，跨章节的可知范围遵循剧情阶段，不因角色拥有更完整的资料而提前获得后续内容。

### 用户档案
- **Definition**：代表一个独立参与者的本地身份档案，以稳定的 `profileId` 区分，并保存该参与者在故事中的专属进度与状态。
- **Rules/Invariants**：每个档案必须有明确的身份标识和可识别名称；档案由后台管理者创建和配置，不使用无法区分的空身份；聊天存在多个档案时由用户自行选择使用的档案。

### 用户剧情进度
- **Definition**：某个用户在目标故事中的当前解锁位置，由一个场景级剧情游标表示。
- **Rules/Invariants**：不同用户的剧情进度彼此独立；后台可以将单一游标向前或向后调整；用户只能获得自己当前游标及其之前已解锁节点的剧情知识；游标回退时，该节点之后产生的用户状态也一并回退。

### 剧情回退
- **Definition**：将用户的剧情游标和体验状态恢复到更早剧情节点的操作。
- **Rules/Invariants**：回退必须同步恢复记忆、好感度、心情、自我认知和对话历史，不保留回退节点之后产生的状态。

### 用户状态隔离
- **Definition**：不同用户档案之间对故事体验状态的独立保存关系。
- **Rules/Invariants**：剧情进度、角色记忆、好感度、心情、自我认知和对话历史全部按用户隔离；新建用户档案从零开始，不自动继承已有用户状态；角色配置可以由多个用户共享，一个用户的状态变化不能直接改变另一个用户的状态。

### 剧情节点顺序
- **Definition**：目标故事中小章节、场景和对话页面之间的先后关系，用来驱动单一剧情游标。
- **Rules/Invariants**：剧情按两大章内部的小章节层级顺序组织；游标以场景 / 页面为解锁节点，节点内保持原文的说话角色和台词顺序。

### 后台剧情管理
- **Definition**：管理者配置用户档案、查看用户状态并手动调整其剧情进度的能力。
- **Rules/Invariants**：第一期与剧情进度相关的用户档案和游标设置都必须能在后台完成和观察，不能要求编辑隐藏配置才能使用；调整进度不等于自动改写角色亲身经历记忆。

### 剧情解锁节点
- **Definition**：目标故事中可独立推进和判定知情范围的最小完整剧情单元，对应一个场景或文本页面。
- **Rules/Invariants**：节点内保留完整说话角色、原文台词和顺序；节点解锁后，角色只能获得该节点内从其入场点开始且允许其知道的内容。

### 角色入场点
- **Definition**：角色在一个剧情场景中首次进入并开始能够听见或参与对话的位置。
- **Rules/Invariants**：角色默认看不到入场点之前的场景对话；只有后续原文明确出现对前情的转述或告知时，角色才可通过该转述获得相关信息。

### 完整剧情文本
- **Definition**：目标故事中按原始顺序保存的全部文本，包括角色台词、旁白、系统提示、选项文本和战斗结果等非对话内容。
- **Rules/Invariants**：每段文本必须保留原文并标注其文本类型；有说话者的内容保留说话角色，非角色内容不能伪装成任何角色的发言。

### 剧情推进方式
- **Definition**：改变用户剧情进度、开放后续剧情知识的规则。
- **Rules/Invariants**：第一期由后台管理者手动推进；自动依据对话或现实时间推进属于后续范围。

### 角色知情范围
- **Definition**：某个角色在特定用户剧情进度和场景入场点下可以使用的目标故事原文集合。
- **Rules/Invariants**：两大章全量原文都可纳入资料，但角色只能接触已解锁、从其入场点开始且符合其角色视角的内容；角色不能把其他角色的台词说成自己的发言，也不能把正史文本自动当作亲身经历记忆。

### 六维人格属性
- **Definition**：描述角色稳定人格倾向的一组 6 个独立数值，用于让角色在不同情境下保持一致且可区分的表现。
- **Rules/Invariants**：每个维度取值为 `0–100`，各维度互不要求总和或互斥；低值、中值和高值都应产生可观察的表现差异。首期属性是角色的固定初始设定，不随对话或剧情成长。

### 外向度
- **Definition**：角色主动接近他人、发起话题和参与互动的倾向。
- **Rules/Invariants**：高值表现为更主动、更愿意延展互动；低值表现为更安静、更倾向等待对方。

### 共情度
- **Definition**：角色识别、理解并回应他人情绪和处境的倾向。
- **Rules/Invariants**：高值更优先回应对方的感受；低值更优先处理事实、自身目标或直接问题。

### 理性度
- **Definition**：角色使用分析、秩序和克制来处理问题的倾向。
- **Rules/Invariants**：高值更重视证据、逻辑和后果；低值更容易依靠直觉、冲动或情绪判断。

### 勇气
- **Definition**：角色面对冲突、未知、风险和压力时采取行动的倾向。
- **Rules/Invariants**：高值更愿意直面困难并承担风险；低值更倾向回避、谨慎或寻求安全路径。

### 好奇度
- **Definition**：角色探索未知、追问细节和尝试新事物的倾向。
- **Rules/Invariants**：高值更主动探索和追问；低值更偏好熟悉、确定和已知内容。

### 独立度
- **Definition**：角色保留自身判断、目标和行动节奏的倾向。
- **Rules/Invariants**：高值更坚持独立判断和自主行动；低值更重视陪伴、依赖和顺应重要关系。

### 属性语义区间
- **Definition**：把数值转成角色可理解的表现强度描述的约定。
- **Rules/Invariants**：数值以连续范围表达强度，同时使用 `0–19` 极低、`20–39` 偏低、`40–59` 中性、`60–79` 偏高、`80–100` 极高的稳定语义区间辅助解释；不能把只有跨过单一阈值才生效作为唯一影响方式。

### 稳定人格与暂态状态
- **Definition**：稳定人格属性描述角色长期倾向；情绪、心情、关系亲密度和自我认知描述角色当前状态或关系历史。
- **Rules/Invariants**：稳定人格属性不替代暂态情绪、心情、关系亲密度或自我认知；这些概念各自表达不同事实。

### 首期属性边界
- **Definition**：第一期只建立固定人格属性及其表现影响，不建立属性成长循环。
- **Rules/Invariants**：角色属性在首期保持固定；对话或剧情产生的变化继续由现有情绪、关系和记忆机制表达；后台只读查看不改变属性。

### 属性影响
- **Definition**：六维人格属性共同调制角色在对话、叙事、反思、日常行为以及情绪和关系事件中的表现。
- **Rules/Invariants**：所有角色使用同一套固定映射规则；每个维度的影响随 `0–100` 数值连续变化，不依赖单一开关阈值。routine 的 `personalityBias` 取 `-1..1`，得分为 `Σ((value-50)/50 × bias)`；事件响应倍率以 50 为中性并限制在 `0.5..1.5`。数值属性不直接替代情绪、心情或关系亲密度。

### 属性缺省值
- **Definition**：未提供某一人格属性时使用的中性人格值。
- **Rules/Invariants**：缺省维度按 `50` 处理；旧式文本 persona 保持原有表现，不因缺少六维属性而改变行为。

### 隐藏人格属性
- **Definition**：用于驱动角色表现、对普通对话参与者隐藏，但允许后台管理者只读检查的角色设定。
- **Rules/Invariants**：聊天和角色回复不直接透露数值设定；后台可以查看属性以进行调试和观察，用户仍主要通过角色的语言、选择倾向和行为差异感知属性影响。

### 确定性行为选择
- **Definition**：在多个日常行为候选中，依据当前状态和人格偏好稳定选出同一结果的规则。
- **Rules/Invariants**：优先匹配当前情绪区间，再比较候选的 `personalityBias` 得分，最后按声明顺序解决平局；缺失人格值按 50；相同输入必须得到相同选择，不使用随机数。

### 属性与现有状态的分工
- **Definition**：人格属性表达长期倾向，现有情绪和关系机制表达事件后的暂态变化。
- **Rules/Invariants**：人格对情绪、关系事件的影响使用统一映射；对话和剧情不在首期直接成长或改写人格属性，关系亲密度仍是独立数值。

### 首期属性可见性
- **Definition**：人格属性对普通参与者保持隐藏，只通过角色表现产生反馈；后台管理者可只读查看。
- **Rules/Invariants**：不提供属性编辑能力；后台查看不改变角色属性，聊天界面不直接展示属性数值。

### 首期兼容约定
- **Definition**：新增人格属性不破坏已有角色配置和调用方式。
- **Rules/Invariants**：旧式文本 persona 和缺少维度的结构化 persona 继续有效；未配置人格偏好的行为候选保持既有声明顺序。

### 当前剧情概述
- **Definition**：依据用户当前剧情进度和指定角色可见剧情，为用户理解当前故事位置而提供的简短剧情说明。
- **Rules/Invariants**：只能基于已解锁且符合角色知情范围的剧情事实；不能泄露未来内容，不能把推测或未知内容当成已发生事实，也不等同于角色的亲身经历记忆。

### 剧情回顾
- **Definition**：当前剧情概述附近已经发生的有限范围剧情节点，用于帮助用户恢复故事上下文。
- **Rules/Invariants**：按剧情节点顺序表达，范围固定为最近 3 个可见节点；只包含当前档案已解锁且角色可知的内容，缺少可见内容时保持明确空状态。

### 剧情提问提示
- **Definition**：围绕当前剧情概述生成的可选问题，帮助用户向角色发起与当前故事相关的对话。
- **Rules/Invariants**：问题必须以当前角色可见的剧情事实为依据，不得预设未解锁事件或角色未知信息；提示是用户输入辅助，不代表用户已经发送或角色已经回答。
